module.exports = {
    secretKey : 'YoUrSeCrEtKeY', // secret Key
    options : {
        algorithm : "HS256",    // hash algorithm
        expiresIn : "30m",      // token expire period
        issuer : "issuer"       // publisher
    }
}