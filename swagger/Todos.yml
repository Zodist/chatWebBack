# /swagger/Todos.yml

components:
  schemas:
    Todos:
      properties:
        todoid:
          type: integer
          description: 번호
        content:
          type: string
          description: 내용
        completed:
          type: boolean
          description: 완료 여부